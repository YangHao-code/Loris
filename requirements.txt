# Core scientific stack
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0

# PyTorch — install with CUDA support separately if needed:
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
torch>=2.1.0

# HuggingFace ecosystem
transformers>=4.40.0
tokenizers>=0.15.0
accelerate>=0.27.0

# PEFT + 4-bit quantisation (required for LoRASLMClassifier)
peft>=0.10.0
bitsandbytes>=0.43.0

# XGBoost (required for PretrainedEncoderClassifier with classifier_head="xgboost")
xgboost>=1.7.0
